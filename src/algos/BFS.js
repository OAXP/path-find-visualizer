import { useAlgo } from "../hooks/AlgoProvider";

export default function BFS() {
    const { grid, setGrid, setRun, start, end } = useAlgo();

    // TODO implement BFS and visualize it and the path found
    // Use previous celle for help
}